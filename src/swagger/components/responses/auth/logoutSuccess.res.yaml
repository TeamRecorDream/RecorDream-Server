description: 로그아웃 성공
content:
  application/json:
    schema:
      $ref: "../../schemas/success/success.schema.yaml"
    example:
      status: 200
      success: true
      message: 로그아웃 성공
