description: 감정 범위(0~6) 벗어났을 경우
content:
  application/json:
    schema:
      $ref: "../../schemas/error/error.schema.yaml"
    example:
      status: 400
      success: false
      message: 감정 범위를 벗어났습니다.
