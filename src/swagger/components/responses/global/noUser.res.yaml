description: 존재하지 않는 유저
content:
  application/json:
    schema:
      $ref: "../../schemas/error/error.schema.yaml"
    example:
      status: 401
      success: false
      message: 존재하지 않는 유저
